import React from 'react';
import { Divider, Grid, Stack, Typography, useMediaQuery, Box, Alert } from '@mui/material';

const QuizDetails = () => {
    return (
        <Box>
            <Grid container spacing={2}>
                <Alert color="warning"></Alert>
            </Grid>
        </Box>
    );
};

export default QuizDetails;
