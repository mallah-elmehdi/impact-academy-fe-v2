import React from 'react';
import { Box, Alert, Stack, Typography, Divider, IconButton } from '@mui/material';

const Footer = () => {
    return (
        <Box display="flex" alignItems="center">
            sa;lam
        </Box>
    );
};

export default Footer;
